# 検証結果 — 2026-09-04

試遊後のタップ判定拡大では、従来の半径外だった30px離れた入力が新ルールで命中し、旧ルールの結果は変化しない回帰テストを追加。自動テストは計17件。

## 自動検証

- `npm test`: 16件合格。JST04:00境界、シード再現、異常5種/16イベント、空間/時刻判定、連打拒否、カード秘匿、Daily二重反映拒否、潜在法則、粒子再挑戦、RC不足、アイコン所有制限、戦闘回数/精算、補正上限、占術入力検証、API同時更新/再送を確認。
- `npm run build`: 成功。ES Modulesの構文検査、公開ファイルのコピーを確認。
- `npm run deploy:check`: Wrangler 4.129.0のWorker/静的ファイルビルド成功。dry-runのみで本番へアップロードしていない。
- `npx wrangler types`: D1/ASSETSの型生成成功。
- `npm run db:local`: ローカルD1に0001 migration適用成功。
- ローカルWorker実行でbootstrapが300 RC、同じ操作キーの召喚を2回送って両方200 RC、二重消費なし。

## ブラウザ

Edge / Playwrightの4シナリオを確認。PC1440pxとスマホ390pxの表示を確認。

1. 初回案内、カードDaily、報酬なし訓練、召喚、戦闘精算、出生情報の端末内保存/削除、予測の開発中表示、8画面の横あふれなし、リロード継続。
2. 潜在法則5問を実時間の順次表示で回答し、結果と報酬を保存。
3. 性能測定3秒と60秒粒子試験を実時間で完了し、結果保存とリプレイ時刻変更。
4. 粒子試験を閉じて中断。RCを増やさず再試行可能。

「開発中」は仕様どおり `aria-disabled` を付けつつモーダルを開くボタン。自動操作側はこのボタンだけ明示的にforce clickを使用。通常操作・保存ボタンは標準の操作可能性確認を使用。
モーダルを閉じた際のフォーカスを修正し、PC/スマホの主導線と粒子中断を再検証済み。

スクリーンショットはローカルの `test-results/screens/` に保存（Git管理外）。実機iPhone/Android、Safari/Firefox、実際の低性能スマホは未検証。低FPS無効化処理は実装済みだが、実機での閾値調整が残る。

## 経済シミュレーション

12キャラそれぞれ1,000シードの初期戦闘を実行。現在の初期3モンスターでは全キャラが勝利する易しい調整。
Daily3種で最大30 RC、通常戦闘5回で最大50 RC、合計80 RC/日。初期300 RC。1回召喚100 RC、10連900 RC。
試遊後に難度・成長速度を調整する前提。訓練によるRC/XP増加なし。予測・レイドによる仮報酬なし。

## 流用元保全

`docs/PRISMA_INTEGRITY.json` のとおり、調査前後8,023ファイルの相対パス・サイズ・更新日時に差分なし。コピーした元マスタ2件と画像27件はSHA-256でも一致。元ディレクトリへの書き込み・削除・移動は実行していない。
